import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'References | ATS | Search.Market',
}

export default function ReferencesLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
